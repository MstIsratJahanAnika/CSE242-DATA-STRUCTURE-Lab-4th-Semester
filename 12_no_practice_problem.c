#include<stdio.h>
int main()
{
    int n, e; //n-size,e-number of elements

    printf("enter the size of array :");
    scanf("%d", &n);

    int arr[n];

    printf("Enter the number of elements :");
    scanf("%d", &e);

    if(e > n)
    {
        printf("number of ");
    }
    return 0; 
}